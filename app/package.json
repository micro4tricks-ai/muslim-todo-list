{
  "name": "muslim-todo-list-app",
  "private": true,
  "version": "1.0.0",
  "description": "Android app shell for Muslim To-Do List (Capacitor). The web files come from the repository root.",
  "scripts": {
    "copy-web": "node scripts/copy-web.mjs",
    "sync": "npm run copy-web && npx cap sync android"
  },
  "dependencies": {
    "@capacitor/android": "8.5.2",
    "@capacitor/core": "8.5.2",
    "@capacitor/local-notifications": "8.3.1"
  },
  "devDependencies": {
    "@capacitor/cli": "8.5.2"
  }
}
